# Custom Chat API Configuration
# IMPORTANT: Use CHAT_API_KEY (without VITE_ prefix) to keep it server-side only
# This prevents the API key from being exposed in the client-side bundle
CHAT_API_URL=https://your-api-endpoint.com/chat
CHAT_API_KEY=your-api-key-here

# Optional: Additional headers for your API (comma-separated key:value pairs)
# CHAT_API_HEADERS=Authorization:Bearer token,X-Custom-Header:value

# Convex configuration
VITE_CONVEX_URL=your-convex-deployment-url-here

# Sentry configuration
VITE_SENTRY_DSN=your-sentry-dsn-here
SENTRY_AUTH_TOKEN=your-sentry-auth-token-here

#Important! Never commit your `.env` file to version control as it contains sensitive information.